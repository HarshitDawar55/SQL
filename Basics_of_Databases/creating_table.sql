-- command for creating a table; 
-- practice is the database name;
-- use name of the table which you want to use create instead of praactice which is mentioned in the command;
-- col1 is the placeholder for the first column name;
-- dt1 is the placeholder for the datatype of 1st column;
-- '...' is for other adding columns, it is not the part of the actual command.

create table practice(col1 dt1, col2 dt2, ...);
