-- min function is used to find the minimum for a particular column of a table;
-- I have used test table name for example;
-- 'price' passed in min represents name of the column, it will find the minimum value from the price column;

-- syntax is:


select min(price) from test;
