-- count function is used to count the rows in a particular table;
-- I have used test table name for example;
-- '*' passed in count represents everything, if we want to count a particluar column we can add the column name instead of that star. 

-- syntax is:


select count(*) from test;
