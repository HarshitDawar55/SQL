-- max function is used to find the maximum for a particular column of a table;
-- I have used test table name for example;
-- 'price' passed in max represents name of the column, it will find the maximum value from the price column;

-- syntax is:


select max(price) from test;
